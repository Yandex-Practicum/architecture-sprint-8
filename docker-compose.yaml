version: '3.8'

services:
  openldap:
    image: osixia/openldap:1.5.0
    container_name: openldap
    command: --copy-service
    volumes:
      - ./ldap/config.ldif:/container/service/slapd/assets/config/bootstrap/ldif/50-bootstrap.ldif
    environment:
      - LDAP_ORGANISATION=example-com
      - LDAP_DOMAIN=example.com
      - LDAP_ADMIN_PASSWORD=admin
    ports:
      - "389:389"
      - "636:636"

  keycloak_db:
    image: postgres:14
    environment:
      POSTGRES_DB: keycloak_db
      POSTGRES_USER: keycloak_user
      POSTGRES_PASSWORD: keycloak_password
    volumes:
      - postgres-keycloak-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  keycloak:
    image: quay.io/keycloak/keycloak:21.1
    environment:
      KC_HOSTNAME_STRICT: "false"
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak_db:5432/keycloak_db
      KC_DB_USERNAME: keycloak_user
      KC_DB_PASSWORD: keycloak_password
    command:
      - start-dev
      - --import-realm
    volumes:
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json
    ports:
      - "8080:8080"
    healthcheck:
      # Keycloak container images do not include `curl`. Use /dev/tcp or a simple script.
      # This example uses a shell command to check the TCP port and HTTP status.
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080; echo -e 'GET /health/ready HTTP/1.1\\r\\nhost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3; if [ $? -eq 0 ]; then echo 'Healthcheck Successful'; exit 0; else echo 'Healthcheck Failed'; exit 1; fi;"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s # Gives Keycloak time to start up before the first check
    depends_on:
      keycloak_db:
        condition: service_started
      openldap:
        condition: service_started

  bionicpro-auth:
    build:
      context: ./bionicpro-auth
      dockerfile: Dockerfile
    ports:
      - "8181:8181"
    environment:
      SPRING_SECURITY_OAUTH2_CLIENT_PROVIDER_KEYCLOAK_AUTHORIZATION_URI: "http://localhost:8080/realms/reports-realm/protocol/openid-connect/auth"
      SPRING_SECURITY_OAUTH2_CLIENT_PROVIDER_KEYCLOAK_TOKEN_URI: "http://keycloak:8080/realms/reports-realm/protocol/openid-connect/token"
      SPRING_SECURITY_OAUTH2_CLIENT_PROVIDER_KEYCLOAK_USER_INFO_URI: "http://keycloak:8080/realms/reports-realm/protocol/openid-connect/userinfo"
      SPRING_SECURITY_OAUTH2_CLIENT_PROVIDER_KEYCLOAK_JWK_SET_URI: "http://keycloak:8080/realms/reports-realm/protocol/openid-connect/certs"
      KEYCLOAK_PROVIDER_AUTHORIZATION_URI: "http://localhost:8080/realms/reports-realm/protocol/openid-connect/auth"
      KEYCLOAK_PROVIDER_TOKEN_URI: "http://keycloak:8080/realms/reports-realm/protocol/openid-connect/token"
      KEYCLOAK_PROVIDER_USER_INFO_URI: "http://keycloak:8080/realms/reports-realm/protocol/openid-connect/userinfo"
      KEYCLOAK_PROVIDER_JWK_SET_URI: "http://keycloak:8080/realms/reports-realm/protocol/openid-connect/certs"
      KEYCLOAK_CLIENT_SECRET: bionicpro-auth-secret-change-in-prod
      APP_FRONTEND_URL: http://localhost:3000
      APP_COOKIE_SECURE: "false"
    depends_on:
      keycloak:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        REACT_APP_AUTH_URL: http://localhost:8181
        REACT_APP_API_URL: http://localhost:8181
    ports:
      - "3000:3000"

volumes:
  postgres-keycloak-data:
