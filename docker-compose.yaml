version: '3.8'

services:
  postgres:
    image: postgres:16.0
    volumes:
      - ./Task2/data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: sample
    logging:
      options:
        max-size: 10m
        max-file: "3"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "airflow"]
      interval: 10s
      retries: 5
      start_period: 5s
    restart: always
  keycloak_db:
    image: postgres:14
    environment:
      POSTGRES_DB: keycloak_db
      POSTGRES_USER: keycloak_user
      POSTGRES_PASSWORD: keycloak_password
    volumes:
      - ./postgres-keycloak-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
  keycloak:
    image: quay.io/keycloak/keycloak:21.1
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak_db:5432/keycloak_db
      KC_DB_USERNAME: keycloak_user
      KC_DB_PASSWORD: keycloak_password
      KC_FEATURES: token-exchange     
      YANDEX_CLIENT_ID: 20966e97364145979a9eecfe895403cd
      YANDEX_CLIENT_SECRET: 97b6bc1107ff4b8c85f5a618ad5c2589
    command: 
      - start-dev
      - --import-realm
    volumes:
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json
    ports:
      - "8080:8080"
    depends_on:
      - keycloak_db
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      REACT_APP_AUTH_URL: http://localhost:5000
      REACT_APP_API_URL: http://localhost:8000
      REACT_APP_KEYCLOAK_URL: http://localhost:8080
      REACT_APP_KEYCLOAK_REALM: reports-realm
      REACT_APP_KEYCLOAK_CLIENT_ID: reports-frontend
  auth:
    build:
      context: ./bionicpro-auth
      dockerfile: ./BionicproAuth.Api/Dockerfile
    ports:
    - "5001:8080"
    environment:
      - Keycloak__Authority:http://keycloak:8080/realms/reports-realm
      - Keycloak__ClientId:reports-frontend
      - Keycloak__RedirectUri:http://localhost:5000/auth/callback
      - Keycloak__PostLogoutRedirectUri:http://localhost:3000
      - Session__CookieName:bionicpro.session
      - Session__CookieDomain:localhost
      - Session__SessionLifetimeMinutes:30
      - ApiProxy__ReportServiceUrl:http://report-service:8000

    depends_on:
      - keycloak
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-data:/data

  minio-setup:
    image: minio/mc:latest
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      sleep 5;
      mc alias set myminio http://minio:9000 minioadmin minioadmin;
      mc mb myminio/reports || true;
      mc anonymous set download myminio/reports;
      exit 0;
      "

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx-cache:/var/cache/nginx
    ports:
      - "8081:80"
    depends_on:
      - minio

volumes:
  minio-data:
  nginx-cache:
